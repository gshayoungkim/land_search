# 문정동 651-3 필지 오선택 디버그 보고서

- 증상: `서울특별시 송파구 문정동 651-3` 검색 후 Kakao 지도 위에 다른 필지가 선택됨.
- 원인: 주소 검색은 정확한 PNU `1171010800106510003`을 반환했지만, 지도 선택 코드는 검색 PNU를 버리고 작은 bbox에 걸린 `lp_pa_cbnd_bonbun` 첫 번째 피처를 무조건 선택했다. 실데이터에서 첫 피처는 본번 641이고 두 번째가 651이었다.
- 추가 확인: `lp_pa_cbnd_bubun` 실데이터에는 6개 후보가 반환됐으며 그중 PNU `1171010800106510003`, 주소 `서울특별시 송파구 문정동 651-3`인 정확한 피처가 존재했다.
- 수정: 검색 시 PNU 정확 일치를 최우선으로 선택하고, PNU가 없는 일반 지도 클릭은 클릭점을 실제로 포함하는 Polygon/MultiPolygon만 선택한다. 상세 부번 레이어를 먼저 조회하고 본번 레이어는 fallback으로만 사용한다. 일괄 캡처의 대상 필지 선택에도 같은 규칙을 적용했다.
- 회귀 테스트: `frontend/map-selection.test.mjs`
- 검증: 합성 회귀 테스트 통과. VWorld 실데이터 6개 후보에 수정 함수를 적용한 결과 PNU 일치 선택과 좌표 포함 선택 모두 PNU `1171010800106510003`, 주소 `서울특별시 송파구 문정동 651-3`으로 확인. Vite 프로덕션 빌드 통과.
- 상태: DONE
